import { createTheme } from "@mui/material";

export const paletaPagination = createTheme({
    palette: {
      primary: {        
        main: '#FFFF',
      },
      secondary: {        
        main: '#FFFF',
      },
    },
  });